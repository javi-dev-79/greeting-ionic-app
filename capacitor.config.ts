import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'greating.ionic.app',
  appName: 'greating-ionic-app',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
